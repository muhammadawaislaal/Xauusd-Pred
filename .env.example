# Backend API Configuration
# The frontend requires a running backend API server for professional trading data
NEXT_PUBLIC_API_URL=http://localhost:5000

# Optional: WebSocket URL for real-time updates
NEXT_PUBLIC_WS_URL=ws://localhost:5000/ws

# Note: This application uses REAL market data only
# No mock data is provided - backend API is required for functionality
